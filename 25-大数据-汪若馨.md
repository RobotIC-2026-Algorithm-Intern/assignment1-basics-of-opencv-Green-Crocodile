用调参小工具获得颜色范围

读取JSON数据（颜色范围、有无球的临界值、ROI）,并将其转换为Python对象
config=json.load(f)

创建添加颜色范围的字典：键：颜色名称（字符串）;值：颜色范围（两个元组）
红色需要两个键值对
self.color_ranges[color] = [tuple(range_data['lower_hsv']), tuple(range_data['upper_hsv'])]

cv2.VideoCapture(video_path)捕获视频
cv2.VideoCapture.get(cv2.CAP_PROP_FPS)获取视频帧率
cv2.CAP_PROP_FRAME_WIDTH ，cv2.CAP_PROP_FRAME_HEIGHT 获取视频宽和高
cv2.VideoCapture.cap.read()读取下一帧，以下是循环处理
frame[crop_top:self.height-crop_top, crop_side:self.width-crop_side]切片获取ROI
尺寸用尺子在电脑屏幕上量的

cv2.cvtColor(roi, cv2.COLOR_BGR2HSV)转换成HSV图像

cv2.inRange(hsv_frame, np.array(lower), np.array(upper))生成掩膜
cv2.bitwise_or(mask1, mask2)为红色生成组合掩膜

cv2.countNonZero(red_mask) / (roi.shape[0] * roi.shape[1])计算掩膜中像素的比例
如果所有指定颜色比例均不超过总像素的0.1（该比例来自deepseek）视作无球
本来准备让绿色最多来代表无球（视频中央有一块常驻区是绿色的），但是有一段绿色和蓝球的颜色太接近了，参数不好调
如果只有一个颜色高于0.1视作该颜色的球
如果有多个颜色则需要再比较

cv2.putText(frame, ball_text, (10, frame.shape[0]-10), cv2.FONT_HERSHEY_SIMPLEX, 2.0, (255,0,0), 3)将结果写入视频

cv2.waitKey(self.wait_time)使视频原速播放

问题1：调颜色参数应该遵循什么样的顺序，一开始发现红球会被检测成紫球，就把紫的范围调小了，结果紫球的时候又检测成蓝球了，以及HSV三个参数调哪一个（感觉不同的HSV组合有可能在一张图中显示出很相似的掩膜）
问题2：忘记用滤波了（